import React from 'react';
import { WhapiChannelConfig } from '../components/WhapiChannelConfig';

export const WhapiConfigPage: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <WhapiChannelConfig />
    </div>
  );
};
